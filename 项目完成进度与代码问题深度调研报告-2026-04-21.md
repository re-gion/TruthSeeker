# TruthSeeker 项目完成进度与代码问题深度调研报告

> **调研日期**: 2026-04-21
> **调研范围**: truthseeker-web（前端）、truthseeker-api（后端）、docs/（文档）、SQL 迁移、测试
> **调研方法**: 4 个并行子代理分别审计后端代码、前端代码、数据流/核心逻辑、文档/测试；主代理执行构建验证和测试运行
> **源文件总数**: 约 135 个 TypeScript/Python 源文件（后端 ~7300 行，前端 ~10900 行）

---

## 第一部分：项目完成进度调研结果

### 总体结论

| 里程碑 | 状态 | 说明 |
|--------|------|------|
| M1 — MVP 可用 | ✅ 完成 | 视频检测 + 双 Agent + SSE 流式推送 |
| M2 — 完整辩论 | ✅ 完成 | 全模态 + 四 Agent + 收敛算法 |
| M3 — 专家会诊 | ✅ 完成 | Realtime 协作、会诊暂停/恢复、3D 可选 |
| M4 — 竞赛就绪 | ❌ 未开始 | 无部署配置、无 CI/CD、无演示准备 |

**项目综合完成度：约 82%**。核心检测流程（上传 → SSE 流式 → 四 Agent 辩论 → 报告生成/分享）链路完整且前后端契约对齐，不是 demo 或原型级别。主要差距在案例库伪实现、部署缺失、部分 UI 细节和文档同步。

---

### 模块完成度明细

#### 1. 前端（完成度 82%）

| 模块 | 完成度 | 分类 | 证据 |
|------|--------|------|------|
| 路由保护 (middleware) | 95% | 真实可用 | Supabase SSR cookie 验证完整，登录/检测页受保护 |
| 认证流程 (login/signup) | 85% | 真实可用 | 注册/登录/登出完整，"忘记密码"和"记住我"空实现 |
| 落地页 | 90% | 真实可用 | 视觉完整，Agent 展示/工作流 Tab/统计计数器均渲染 |
| 检测首页 (上传) | 80% | 真实可用 | 多文件上传、类型校验、拖拽上传完整；进度条是模拟的 |
| 检测控制台 (4 Agent) | 90% | 真实可用 | SSE 17 种事件全部对接，Agent 状态可视化，PDF/MD 下载 |
| 仪表盘 (大屏) | 95% | 真实可用 | 6 种 ECharts 图表，数据层有 fallback，三层展示完善 |
| 案例库 | 40% | **只有壳子** | DemoCaseSelector 跳转到 `/detect?demo=X`，但首页不读取 demo 参数 |
| 专家会诊 | 85% | 真实可用 | Realtime Presence + Broadcast、专家面板、邀请码机制完整 |
| 3D 场景 (Bento) | 90% | 真实可用 | R3F Canvas + 光照 + 动画完整，CSS glassmorphism 实现 |
| 报告分享页 | 90% | 真实可用 | Markdown 表格渲染、hash 展示、API 对接完整 |
| 工具库 (lib/) | 95% | 真实可用 | 纯函数有测试覆盖，dashboard/report 数据规范化完善 |

**前端构建验证**: `next build` 成功编译，TypeScript 无报错，11 个页面全部生成。

#### 2. 后端（完成度 82%）

| 模块 | 完成度 | 分类 | 证据 |
|------|--------|------|------|
| API 路由 | 95% | 真实可用 | 7 个路由模块（upload/tasks/detect/report/share/consultation/dashboard） |
| Agent 辩论系统 | 90% | 真实可用 | LangGraph 四节点拓扑 + 条件边 + fan-in/fan-out 完整运行 |
| 外部 API 集成 | 85% | 真实可用 | Reality Defender 三步流程 + VirusTotal v3 完整；无 key 时降级到 mock |
| LLM 推理 | 85% | 真实可用 | 四 Agent 专用 LLM 函数 + Kimi/Moonshot 集成；fallback 文本完整 |
| 认证授权 | 80% | 真实可用 | JWT 验证 + 公开/受保护路由分离 + 任务所有权校验 |
| 数据持久化 | 90% | 真实可用 | 9 张 Supabase 表全覆盖，best-effort 不阻塞主流程 |
| 降级/容错 | 90% | 真实可用 | 三级降级（full→degraded→minimal），DegradationManager 全局共享 |
| 报告生成 | 100% | 真实可用 | MD + PDF（WeasyPrint）双格式，SHA-256 hash 完整性校验 |
| 审计日志 | 100% | 真实可用 | 11 种操作类型全覆盖，敏感字段自动脱敏 |
| 安全性 | 75% | 部分可用 | MIME 校验+大小限制+路径穿越防护均有，但存在安全隐患（见第二部分） |

**后端测试验证**: `pytest` 77 项全部通过（5.79s），含纯函数、API 契约、降级管理、报告完整性、认证配置、文本校验、会诊恢复。

#### 3. 数据库（完成度 90%）

| 项目 | 状态 | 说明 |
|------|------|------|
| Schema（9 表） | ✅ 完整 | profiles/tasks/analysis_states/agent_logs/reports/consultation_invites/consultation_messages/audit_logs/system_stats |
| RLS 策略 | ✅ 完整 | 全部 9 表 RLS 启用，使用 `(select auth.uid())` initplan 优化 |
| 迁移文件 | ⚠️ 冗余 | baseline 与增量迁移存在大量重复建表（`if not exists` 保证幂等） |
| 字段一致性 | ✅ 一致 | 代码中使用的字段名/类型与 SQL schema 完全匹配 |

#### 4. 文档（完成度 65%）

| 文档 | 状态 | 说明 |
|------|------|------|
| PRD.md | 过时 | 最后更新 2026-03-01，候选服务列表与实际不符 |
| TECH_STACK.md | ✅ 同步 | 与 package.json 一致 |
| BACKEND_STRUCTURE.md | 基本同步 | 缺少 text_validation.py 说明 |
| FRONTEND_GUIDELINES.md | 过时 | 提到不存在的 `@react-spring/three` 依赖 |
| APP_FLOW.md | ✅ 同步 | 已反映最新流程 |
| IMPLEMENTATION_PLAN.md | 过时 | 附录版本号严重过时，环境变量缺失 |
| CLAUDE.md | **已修复** | 版本号和环境变量名已更新（本次小修） |

---

### 数据流贯通验证

| 链路 | 完整性 | 说明 |
|------|--------|------|
| 文件上传 | 95% | 前端→后端→Supabase Storage→签名 URL，magic bytes + MIME 双重校验 |
| 任务创建 | 100% | POST /tasks→Supabase tasks 表→task_id 返回，前后端契约完全匹配 |
| 检测流程（SSE） | 95% | 17 种 SSE 事件类型前后端完全匹配，LangGraph 拓扑正确 |
| 报告生成 | 100% | MD/PDF 双格式，hash 完整性，前后端下载路径匹配 |
| 会诊流程 | 90% | 邀请/注入/恢复链路完整，恢复后跳过 Challenger 直接裁决 |
| Dashboard | 100% | 公开聚合 API，前后端字段映射完整，降级 fallback 完善 |
| 认证链路 | 95% | JWT 验证正确（含 audience），公开/受保护路由划分合理 |

---

## 第二部分：源代码和文档问题汇报

### 问题按严重程度排序

#### P0 级（阻塞性/数据安全）— 0 项

无 P0 级问题。所有核心链路可运行。

#### P1 级（功能缺陷/安全隐患）— 20 项

| # | 类别 | 模块 | 文件 | 问题 | 影响 |
|---|------|------|------|------|------|
| P1-1 | 安全 | 后端 | `middleware/rate_limit.py` L11-23 | 内存限流器无 key 过期清理，长时间运行会内存泄漏；`request.client.host` 在反向代理后获取代理 IP | 限流失效 + 内存泄漏 |
| P1-2 | 安全 | 后端 | `utils/supabase_client.py` L24 | 使用 service_role key 绕过所有 RLS，完全依赖应用层权限检查 | 新增端点遗漏 owner 检查即越权 |
| P1-3 | 安全 | 后端 | `middleware/auth.py` L111-128 | 公开路由 `_try_extract_user` 对伪造 token 不报错，可能产生伪认证身份 | 认证绕过风险 |
| P1-4 | 安全 | 后端 | `api/v1/report.py` L17-18 | `_assert_task_owner` 在 user_id 为 anonymous 时直接放行 | 匿名用户可访问任何报告 |
| P1-5 | 安全 | 后端 | `api/v1/dashboard.py` L489-542 | Dashboard overview 完全公开无认证，暴露聚合统计 | 信息泄露（非任务详情） |
| P1-6 | 安全 | 后端 | `api/v1/consultation.py` L227-241 | invite token 未标记 "used"，同一邀请码可无限使用 | 邀请机制失效 |
| P1-7 | 功能 | 后端 | `agents/tools/llm_client.py` L21-29 | 每次调用创建新 ChatOpenAI 实例，无连接池复用 | 高并发下 TCP 连接爆炸 |
| P1-8 | 功能 | 后端 | `agents/tools/deepfake_api.py` L275 | Mock 数据偏向高风险（0.3-0.95），永远不返回"安全"结论 | 演示时检测结果失真 |
| P1-9 | 功能 | 后端 | `agents/tools/text_detection.py` | `analyze_text` 函数已实现但未被任何 Agent 节点调用 | 文本 AI 检测能力浪费 |
| P1-10 | 功能 | 后端 | `agents/nodes/osint.py` L99, `challenger.py` L58 | 同步 Supabase 调用在异步 LangGraph 节点中阻塞事件循环 | SSE 流延迟 |
| P1-11 | 功能 | 后端 | `agents/tools/llm_client.py` L22-29 + config.py L21 | KIMI_API_KEY 为空时 LLM 静默降级，用户不知道检测质量下降 | 隐性降级 |
| P1-12 | 功能 | 后端 | `api/v1/tasks.py` L75-93 | evidence_files 同时存入 metadata 和 storage_paths，冗余存储 | 数据冗余 |
| P1-13 | 功能 | 前端 | `components/demo/DemoCaseSelector.tsx` L74 | 点击案例跳转到 `/detect?demo=X` 但首页不读取 demo 参数 | 案例库伪实现 |
| P1-14 | 功能 | 前端 | `components/detect/DetectConsole.tsx` L407/443/554 | timeline 视图同时渲染 2D grid 和 timeline，双重内容显示 | 视觉 bug |
| P1-15 | 功能 | 前端 | `app/detect/HomeClientWidgets.tsx` L39-40 | 硬编码 `text-white` 和灰色文字，浅色主题下不可见 | 主题适配缺陷 |
| P1-16 | 功能 | 前端 | 多个文件 | `getAuthToken()` 在 DetectConsole/ExpertPanel/InviteButton 各自定义一份 | 代码重复 |
| P1-17 | 功能 | 前端 | `components/demo/ComparisonView.tsx` L79/100 | 引用 Unsplash 外部图片，需要 next.config remotePatterns 且竞赛环境可能不可达 | 演示风险 |
| P1-18 | 功能 | 前端 | `hooks/useAgentStream.ts` L327-335 | expert 模式 useEffect cleanup 不完整，channel.on 回调可能未清理 | 内存泄漏 |
| P1-19 | 文档 | 根目录 | `CLAUDE.md` | 版本锁定表严重过时（Next.js 15→16，motion 12.9→12.34 等），环境变量名过时 | **已修复（本次小修）** |
| P1-20 | 文档 | docs/ | `IMPLEMENTATION_PLAN.md` 附录 | 版本号和环境变量与实际严重不符，包含不存在的 DEEPWARE_API_KEY | 文档误导 |

#### P2 级（改进建议）— 28 项

| # | 类别 | 模块 | 文件 | 问题 |
|---|------|------|------|------|
| P2-1 | 安全 | 后端 | `api/v1/share.py` L55 | 分享链接无过期时间，永久有效 |
| P2-2 | 安全 | 后端 | `api/v1/upload.py` L56-60 | 所有匿名用户文件存储在同一个 `anon/` 目录 |
| P2-3 | 安全 | 后端 | `main.py` L68-73 | CORS `allow_methods=["*"]` 和 `allow_headers=["*"]` 过于宽松 |
| P2-4 | 功能 | 后端 | `agents/edges/conditions.py` L5-50 | `should_converge` 函数是死代码，未被 challenger_route 调用 |
| P2-5 | 功能 | 后端 | `agents/nodes/challenger.py` L189-192 | 最多打回一次（round==1），限制辩论深度 |
| P2-6 | 功能 | 后端 | `graph.py` L64 | MemorySaver 非持久化，进程重启状态丢失 |
| P2-7 | 功能 | 后端 | `agents/nodes/osint.py` L48/123 | 人为 `asyncio.sleep(0.3/0.5)` 延迟 |
| P2-8 | 功能 | 后端 | `services/analysis_persistence.py` L304-326 | `_safe_insert` 等方法吞掉所有异常（仅 warning 日志） |
| P2-9 | 功能 | 后端 | `services/report_generator.py` L189-217 | WeasyPrint 在 Windows 上需要 GTK 运行时，部署困难 |
| P2-10 | 功能 | 后端 | `detect.py` L53 | `DetectRequest.user_id` 字段允许前端传入但实际被忽略，有误导性 |
| P2-11 | 功能 | 后端 | `detect.py` L60 | `_sse` 中 `replace("\n", "\\n")` 修改了数据内容 |
| P2-12 | 功能 | 后端 | `config.py` L27-28 | OPENAI_API_KEY 和 QWEN_API_KEY 声明但未使用 |
| P2-13 | 功能 | 后端 | `main.py` L81-83 | `/health` 不检查 Supabase 等依赖服务可用性 |
| P2-14 | SQL | 迁移 | baseline vs 增量迁移 | 3 张表被多个迁移重复 CREATE（if not exists 保证幂等但冗余） |
| P2-15 | SQL | 迁移 | `audit_logs.user_id` (text) vs `tasks.user_id` (uuid) | 类型不一致 |
| P2-16 | 功能 | 前端 | `app/layout.tsx` | 同一字体文件 ZhengKaiTi.ttf 加载两次 |
| P2-17 | 功能 | 前端 | `app/login/page.tsx` | 缺少 ThemeToggle，"忘记密码"和"记住我"空实现 |
| P2-18 | 功能 | 前端 | `components/bento/BentoScene.tsx` L303 | dangerouslySetInnerHTML 注入全局 CSS 动画 |
| P2-19 | 功能 | 前端 | `components/upload/FileUploader.tsx` L159 | 上传进度按文件数量线性模拟，非真实进度 |
| P2-20 | 功能 | 前端 | `hooks/useAgentStream.ts` L339-341 | host 模式 cleanup 不重置 startedRef，组件重挂载时 start() 被跳过 |
| P2-21 | 功能 | 前端 | `hooks/useRealtimeSession.ts` L17-23 | Presence 使用 localStorage 随机 UUID 而非 Supabase Auth UID |
| P2-22 | 功能 | 前端 | `lib/supabase/client.ts` | 每次调用 createClient() 都创建新实例，应使用单例 |
| P2-23 | 功能 | 前端 | `package.json` | `ogl` 依赖已安装但整个代码库未使用 |
| P2-24 | 文档 | docs/ | `FRONTEND_GUIDELINES.md` | 提到 `@react-spring/three` 但项目未安装，实际使用 motion + gsap |
| P2-25 | 文档 | docs/ | `BACKEND_STRUCTURE.md` | 缺少 `text_validation.py` 说明 |
| P2-26 | 文档 | docs/ | `PRD.md` | 最后更新 2026-03-01，候选服务列表与实际不符 |
| P2-27 | 部署 | 项目 | 无 Dockerfile / docker-compose / CI 配置 | M4 阶段必需 |
| P2-28 | 测试 | 项目 | 前端无组件测试和 hook 测试 | 零组件级测试覆盖 |

---

### 已顺手修复的小问题

| # | 文件 | 修改内容 | 修复原因 | 验证方式 |
|---|------|----------|----------|----------|
| 1 | `CLAUDE.md` | 更新版本锁定表：Next.js 15→16.1.6，motion 12.9→12.34，@supabase/ssr 0.5→0.8，drei 10.0→10.7 | 文档与 package.json/TECH_STACK.md 严重不一致 | 人工核对比对 |
| 2 | `CLAUDE.md` | 环境变量名：`NEXT_PUBLIC_SUPABASE_ANON_KEY` → `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`；后端添加 `SUPABASE_JWT_SECRET`、`KIMI_API_KEY` 替换 `OPENAI_API_KEY`/`QWEN_API_KEY` | 环境变量名与实际代码和 .env.example 不一致 | 人工核对比对 |
| 3 | `CLAUDE.md` | 当前进度更新：M3 标记为已完成 | M3 已在 task.md 中确认完成但 CLAUDE.md 未同步 | 与 task.md 一致 |
| 4 | `CLAUDE.md` | 关键规则第 7 条：Next.js 15 → 16 | 实际版本已升级 | 与 package.json 一致 |

**小修风险评估**：所有修改仅涉及文档文字，不改变任何代码逻辑、接口或配置。无后续风险。

---

### 2026-04-21 系统修复记录

基于本报告的问题清单，已执行以下修复：

#### 后端安全修复（8 个文件）

| # | 问题编号 | 文件 | 修复内容 |
|---|----------|------|----------|
| 1 | P1-1 | `middleware/rate_limit.py` | 添加过期 key 清理机制防止内存泄漏；优先使用 X-Forwarded-For 获取真实 IP |
| 2 | P1-4 | `api/v1/report.py` | `_assert_task_owner` 在 user_id 为空/anonymous 时返回 401 |
| 3 | P1-5 | `middleware/auth.py` | 从公开路由中移除 dashboard，需认证才能访问 |
| 4 | P1-6 | `api/v1/consultation.py` | invite token 验证后标记为 used，防止无限重用 |
| 5 | P2-3 | `main.py` | CORS methods 限制为 GET/POST/PUT/DELETE/OPTIONS，headers 限制为 Authorization/Content-Type |
| 6 | P2-4 | `agents/edges/conditions.py` | 为 `should_converge` 死代码添加注释说明 |
| 7 | P2-7 | `agents/nodes/osint.py` | 删除 asyncio.sleep(0.3) 和 asyncio.sleep(0.5) 人为延迟 |
| 8 | P2-8 | `services/analysis_persistence.py` | 日志级别 warning→error，增加详细信息 |
| 9 | P2-10 | `api/v1/detect.py` | 移除 DetectRequest 中误导性的 user_id 字段 |
| 10 | P2-12 | `config.py` | 为未使用的 OPENAI_API_KEY/QWEN_API_KEY 添加注释 |

#### 后端功能修复（4 个文件）

| # | 问题编号 | 文件 | 修复内容 |
|---|----------|------|----------|
| 1 | P1-7 | `agents/tools/llm_client.py` | LLM 客户端按 model_name 缓存单例，避免每次创建新连接 |
| 2 | P1-8 | `agents/tools/deepfake_api.py` | mock 分数范围从 0.3-0.95 改为 0.1-0.95，可返回安全判定 |
| 3 | P1-9 | `agents/nodes/osint.py` | 接入 text_detection.py 的 analyze_text，文本文件执行完整 AI 分析 |
| 4 | P1-10 | `agents/nodes/osint.py` + `challenger.py` | 同步 Supabase 调用改用 asyncio.to_thread 包装 |
| 5 | P1-11 | `agents/tools/llm_client.py` + 节点文件 | LLM 降级时统一添加 `[降级模式: LLM不可用]` 标记 |

#### 前端修复（10 个文件）

| # | 问题编号 | 文件 | 修复内容 |
|---|----------|------|----------|
| 1 | P1-14 | `components/detect/DetectConsole.tsx` | timeline 模式不再同时渲染 2D grid |
| 2 | P1-15 | `app/detect/HomeClientWidgets.tsx` | 文字颜色适配 dark/light 主题 |
| 3 | P1-16 | 新建 `lib/auth.ts` + 3 个组件 | getAuthToken 去重到共享模块 |
| 4 | P1-18 | `hooks/useAgentStream.ts` | expert 模式 cleanup 确保总返回清理函数 |
| 5 | P2-16 | `app/layout.tsx` | 移除重复的 ZhengKaiTi 字体引用 |
| 6 | P2-17 | `app/login/page.tsx` | 添加 ThemeToggle、"忘记密码"标注暂未开放 |
| 7 | P2-20 | `hooks/useAgentStream.ts` | host 模式 cleanup 重置 startedRef |
| 8 | P2-22 | `lib/supabase/client.ts` | Supabase 客户端改用模块级单例缓存 |
| 9 | P2-23 | `package.json` | 移除未使用的 ogl 依赖 |
| 10 | 类型修复 | `hooks/useRealtimeSession.ts` | subscribe 回调参数添加 string 类型注解 |

#### 测试适配（2 个文件）

| # | 文件 | 修复内容 |
|---|------|----------|
| 1 | `tests/test_dashboard_api.py` | dashboard 需认证后 patch `_is_public` 让测试通过 |
| 2 | `tests/test_report_and_consultation_api.py` | PDF 测试 patch `_assert_task_owner` 跳过认证 |

#### 文档更新（5 个文件）

| # | 文件 | 更新内容 |
|---|------|----------|
| 1 | `CLAUDE.md` | 版本号同步、环境变量名更新、M3 标记完成 |
| 2 | `docs/BACKEND_STRUCTURE.md` | 补充 text_validation.py 说明 |
| 3 | `docs/IMPLEMENTATION_PLAN.md` | 附录版本号同步、环境变量模板更新 |
| 4 | `docs/FRONTEND_GUIDELINES.md` | @react-spring/three→motion+gsap |
| 5 | `docs/PRD.md` | 删除已取消功能（自建模型/管理后台等）、更新 LLM 为实际使用的 Kimi/Moonshot、更新日期 |

#### 验证结果

- 后端 pytest: **77 passed** (5.30s)
- 前端 next build: **Compiled successfully** (4.9s)

#### 经分析跳过（无需修改）

| 问题编号 | 原因 |
|----------|------|
| P1-3 | auth.py 的 `_try_extract_user` 已使用 audience 验证，伪认证风险不存在 |
| P2-11 | SSE 的 newline 替换是协议要求的单行化，行为正确 |

#### 用户决策跳过

| 问题编号 | 原因 |
|----------|------|
| P1-13 | 案例库相关暂不修复 |
| P1-17 | 保留外部 Unsplash 图片 |
| P2-27 | 部署配置暂不开发 |

---

### 重大问题和建议处理顺序

#### 需要用户介入/决策的问题

1. ~~**Dashboard 公开访问策略**（P1-5）：已修复，需认证~~
2. ~~**Mock 数据偏向高风险**（P1-8）：已修复，mock 范围改为 0.1-0.95~~
3. **案例库伪实现**（P1-13）：DemoCaseSelector 跳转不生效，暂不修复
4. **WeasyPrint Windows 兼容**（P2-9）：Windows 上需要 GTK 运行时，需确认演示环境是否有 GTK
5. **部署配置缺失**（P2-27）：暂不开发

#### 剩余未修复项

| 问题 | 状态 | 原因 |
|------|------|------|
| P1-13 案例库伪实现 | 跳过 | 用户决策暂不修复 |
| P1-17 ComparisonView 外部图片 | 跳过 | 用户决策保留 |
| P2-9 WeasyPrint Windows | 待确认 | 需要 GTK 运行时 |
| P2-27 部署配置 | 跳过 | 暂不开发 |
| P2-28 前端组件测试 | 长期 | 无短期计划 |
2. Mock 数据增加安全判定分支（避免演示时失真）
3. 添加 Dockerfile（至少后端 + 前端）
4. 修复 DetectConsole timeline 双重渲染

**中期（竞赛后优化）**：
5. rate_limit.py 改用 Redis 或添加 key 过期清理
6. LLM 客户端添加连接池
7. Agent 节点中的同步 Supabase 调用改为 `asyncio.to_thread`
8. text_detection.py 的 analyze_text 接入 Agent 节点

**长期（生产化）**：
9. Supabase 客户端改用 anon key + RLS（而非 service_role key）
10. MemorySaver 改为 PostgresSaver
11. 添加 CI/CD 配置和端到端测试

---

### 实际运行过的验证命令和结果

| 验证项 | 命令 | 结果 | 说明 |
|--------|------|------|------|
| 后端单元测试 | `python -m pytest tests/ --tb=no -q` | **77 passed, 2 warnings** (5.79s) | 全部通过 |
| 前端构建 | `npx next build` | **Compiled successfully** (4.7s) | TypeScript 无报错，11 页面全部生成 |
| 前端路由 | 构建输出确认 | `/ /cases /dashboard /detect /detect/[taskId] /login /signup /report/[taskId]` | 路由完整 |
| 前端依赖安装 | `package.json` 分析 | 所有依赖声明完整 | `ogl` 未使用但已安装 |
| SQL 迁移与代码一致性 | 人工比对 | 字段名/类型完全匹配 | 存在冗余建表 |

---

### 仓库卫生问题

| 问题 | 严重程度 | 建议 |
|------|----------|------|
| `.claude/settings.local.json` 已修改但未忽略 | 低 | 添加到 .gitignore 或撤销 |
| `.pytest_cache/` 目录存在于仓库 | 低 | `git rm -r --cached .pytest_cache` |
| 无 `.editorconfig` 或格式化配置 | 低 | 可选添加 |
| `codexnpmdiag4cf2fcb3bcc44b6a83c145d9d18cc2c6/` 临时目录 | 中 | 确认是否为残留，应删除并加入 .gitignore |
| `skills-lock.json` 在根目录 | 低 | 确认是否需要版本控制 |

---

*报告结束。所有结论均基于 2026-04-21 仓库代码快照，以实际文件内容和构建/测试结果为证据。*
