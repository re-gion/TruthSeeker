[pytest]
asyncio_mode = auto
# Windows 系统 Temp 下可能残留无访问权限的 pytest-of-user 目录（曾以管理员/其他身份运行过 pytest），
# 默认 basetemp 落在系统 Temp 时，tmp_path 会在收集阶段报 WinError 5 导致 ERROR。
# 固定使用项目内可写目录作为 basetemp（已在根 .gitignore 中忽略），彻底规避。
addopts = --basetemp=.pytest_tmp
