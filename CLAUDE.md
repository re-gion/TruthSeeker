@AGENTS.md

## Claude Code Notes

- Claude Code reads `CLAUDE.md`; this file intentionally imports `AGENTS.md` so Codex, Copilot, Claude, and other agents share the same repository guidance.
- Keep Claude-specific notes here only when they are not useful to other tools. Project facts, commands, architecture, and safety rules belong in `AGENTS.md` or nested `AGENTS.md` files.
- On Windows, prefer the import above over a symbolic link.
